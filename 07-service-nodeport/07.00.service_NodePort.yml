apiVersion: v1
kind: Service
metadata:
    name: myapp-service
spec:
    type: NodePort
    selector:
        myPodLabel: myPodValue
    ports:
        - port: 80 #Service port
          targetPort: 80 #Pod port
          nodePort: 30088 #Node port
